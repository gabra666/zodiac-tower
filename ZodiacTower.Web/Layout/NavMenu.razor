<div class="brand-row navbar navbar-dark">
    <div class="container-fluid">
        <a class="navbar-brand" href=""><span class="brand-mark">ZT</span><span>Zodiac Tower</span></a>
        <button title="Abrir navegacion" class="navbar-toggler" @onclick="ToggleNavMenu">
            <span class="navbar-toggler-icon"></span>
        </button>
    </div>
</div>

<div class="@NavMenuCssClass nav-scrollable" @onclick="ToggleNavMenu">
    <nav class="nav flex-column">
        <div class="nav-section-label">Herramientas</div>
        <div class="nav-item">
            <NavLink class="nav-link" href="" Match="NavLinkMatch.All">
                <span class="nav-glyph" aria-hidden="true">G</span> Generador
            </NavLink>
        </div>
        <div class="nav-item">
            <NavLink class="nav-link" href="juego">
                <span class="nav-glyph" aria-hidden="true">J</span> Juego
            </NavLink>
        </div>
        <div class="nav-item">
            <NavLink class="nav-link" href="duelo">
                <span class="nav-glyph" aria-hidden="true">D</span> Duelo
            </NavLink>
        </div>
        <div class="nav-item">
            <NavLink class="nav-link" href="analisis">
                <span class="nav-glyph" aria-hidden="true">A</span> Analisis
            </NavLink>
        </div>
    </nav>
</div>

@code {
    private bool collapseNavMenu = true;
    private string? NavMenuCssClass => collapseNavMenu ? "collapse" : null;
    private void ToggleNavMenu() => collapseNavMenu = !collapseNavMenu;
}
